name: Verify plugin

on:
  push:
    tags:
      - "v*"

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: python3 -m pip install PyYAML
      - run: npm run build
      - run: npm run check
      - run: git diff --exit-code -- dist/server.js dist/hook.js
